// Define the configuration object for the Shopify app
const shopifySchema = {
  // Settings for the image slider
  sliderSettings: {
    // Enable navigation controls (next/prev buttons)
    navigation: true,
    // Enable pagination controls (dots)
    pagination: true,
    // Space between slides in pixels
    spaceBetween: 50,
    // Number of slides to show per view
    slidesPerView: 1,
  },
};

// Export the configuration object as the default export
export default shopifySchema;
